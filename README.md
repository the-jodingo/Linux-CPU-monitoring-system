# Linux-CPU-monitoring-system
A simple Bash script (no dependencies, quick to run)

# How to use :  
   chmod +x cpu-monitor.sh
   
  ./cpu-monitor.sh

  
This is lightweight and works on any Linux distro without extra packages (though sysstat improves per-core accuracy: sudo apt install sysstat on Debian/Ubuntu).


  
